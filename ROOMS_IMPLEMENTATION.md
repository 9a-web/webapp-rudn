# 🏠 Реализация функции "Комнаты с групповыми задачами"

## ✅ Что реализовано

### Backend (Python/FastAPI)

**Модели данных** (`/app/backend/models.py`):
- ✅ `Room` - модель комнаты с участниками
- ✅ `RoomParticipant` - участник комнаты (owner/member)
- ✅ `RoomCreate` - модель создания комнаты
- ✅ `RoomResponse` - ответ с данными комнаты
- ✅ `RoomInviteLinkResponse` - ссылка-приглашение
- ✅ `RoomJoinRequest` - запрос присоединения
- ✅ `RoomTaskCreate` - создание задачи в комнате
- ✅ Обновлена модель `GroupTask` с полем `room_id`

**API Endpoints** (`/app/backend/server.py`):
1. ✅ `POST /api/rooms` - создать комнату
2. ✅ `GET /api/rooms/{telegram_id}` - получить все комнаты пользователя
3. ✅ `GET /api/rooms/detail/{room_id}` - детали комнаты
4. ✅ `POST /api/rooms/{room_id}/invite-link` - сгенерировать ссылку-приглашение
5. ✅ `POST /api/rooms/join/{invite_token}` - присоединиться по токену
6. ✅ `POST /api/rooms/{room_id}/tasks` - создать задачу в комнате
7. ✅ `GET /api/rooms/{room_id}/tasks` - получить все задачи комнаты
8. ✅ `DELETE /api/rooms/{room_id}/leave` - покинуть комнату
9. ✅ `DELETE /api/rooms/{room_id}` - удалить комнату (только владелец)

**Ключевые особенности Backend:**
- 🔗 Автоматическое добавление участников комнаты во все задачи
- 📊 Подсчет прогресса комнаты (завершенные/общие задачи)
- 🎯 Генерация ссылок с реферальным кодом: `https://t.me/{bot_username}?start=room_{token}_ref_{user_id}`
- 🔐 Проверка прав доступа (владелец/участник)
- ♻️ Каскадное удаление задач при удалении комнаты

### Frontend (React)

**API Service** (`/app/frontend/src/services/roomsAPI.js`):
- ✅ 9 методов для работы с API комнат
- ✅ Автоопределение backend URL
- ✅ Обработка ошибок

**Компоненты**:

1. **RoomCard** (`/app/frontend/src/components/RoomCard.jsx`):
   - Карточка комнаты 160x200px
   - Синий градиент (from-blue-50 to-indigo-50)
   - Круговой прогресс выполнения задач
   - Счетчик участников и задач
   - Анимации при hover и tap

2. **CreateRoomModal** (`/app/frontend/src/components/CreateRoomModal.jsx`):
   - Модальное окно создания комнаты
   - Адаптивное (мобильное/десктоп)
   - Валидация названия (до 50 символов)
   - Описание (до 200 символов, необязательно)
   - Haptic feedback
   - Spring анимация

3. **RoomDetailModal** (`/app/frontend/src/components/RoomDetailModal.jsx`):
   - Детальная информация о комнате
   - Список всех задач комнаты с прогрессом
   - Быстрое добавление задач
   - Кнопки: "Пригласить", "Удалить" (владелец) / "Выйти" (участник)
   - Генерация и копирование ссылки-приглашения
   - Отображение статусов задач (completed, overdue, in_progress)
   - Приоритеты задач (high, medium, low)

**Интеграция** (`/app/frontend/src/components/TasksSection.jsx`):
- ✅ Секция "Комнаты" после карточки "Сегодня"
- ✅ Горизонтальный скролл с карточками комнат
- ✅ Кнопка "+ Создать комнату"
- ✅ Автозагрузка комнат при монтировании
- ✅ Обновление состояния модальных окон в родителе

---

## 🎨 Визуальный дизайн

### Цветовая схема
- **Комнаты**: Синий градиент (#4F46E5 → #7C3AED)
- **Карточки**: from-blue-50 to-indigo-50
- **Кнопки**: from-blue-500 to-indigo-600
- **Тени**: shadow-blue-500/20

### Иконки
- **Комната**: Users (👥) из lucide-react
- **Создать**: Plus
- **Поделиться**: Share2
- **Удалить**: Trash2
- **Задачи**: Check, Flag, Calendar

### Размеры
- **Карточка комнаты**: 160px x 200px
- **Круговой прогресс**: 64px (w-16 h-16)
- **Иконка комнаты**: 32px (w-8 h-8)
- **Border radius**: 24px (rounded-[24px])

---

## 📱 Адаптивность

### Мобильные устройства (< 640px)
- Модальные окна открываются снизу (rounded-t-[32px])
- Максимальная высота: 92vh
- Touch-friendly кнопки (touch-manipulation)
- Активные состояния вместо hover (active:scale-95)
- Swipe to dismiss для модальных окон

### Десктоп (≥ 640px)
- Модальные окна центрированы
- Максимальная высота: 85vh
- Hover эффекты
- Увеличенные иконки и текст

---

## 🔄 Пользовательские сценарии

### Сценарий 1: Создание комнаты
1. Пользователь кликает "+ Создать комнату"
2. Открывается модальное окно CreateRoomModal
3. Вводит название (обязательно, до 50 символов)
4. Вводит описание (необязательно, до 200 символов)
5. Кликает "Создать"
6. Комната создается, пользователь становится владельцем
7. Новая карточка комнаты появляется в списке

### Сценарий 2: Приглашение в комнату
1. Владелец/участник открывает детали комнаты (клик на карточку)
2. Кликает кнопку "Пригласить"
3. Генерируется уникальная ссылка: `https://t.me/rudn_pro_bot?start=room_ABC123_ref_USER_ID`
4. Ссылка копируется в буфер обмена
5. Пользователь делится ссылкой через Telegram/мессенджеры
6. Новый пользователь переходит по ссылке → присоединяется к комнате
7. Автоматически добавляется во все существующие задачи комнаты

### Сценарий 3: Создание задачи в комнате
1. Пользователь открывает детали комнаты
2. Кликает "Добавить задачу"
3. Вводит название задачи
4. Кликает "Добавить"
5. Задача создается и автоматически добавляет всех участников комнаты
6. Обновляется прогресс комнаты

### Сценарий 4: Удаление комнаты
1. Владелец открывает детали комнаты
2. Кликает "Удалить"
3. Подтверждает удаление
4. Удаляются: комната, все задачи, все комментарии
5. Карточка исчезает из списка

### Сценарий 5: Выход из комнаты
1. Участник (не владелец) открывает детали комнаты
2. Кликает "Выйти"
3. Подтверждает выход
4. Удаляется из комнаты и всех задач
5. Комната исчезает из его списка

---

## 🔗 Интеграция с Telegram

### Реферальные ссылки
- Формат: `https://t.me/{bot_username}?start=room_{invite_token}_ref_{referral_code}`
- Название бота получается через Telegram Bot API (GET /api/bot-info)
- Реферальный код = telegram_id пригласившего пользователя
- Токен комнаты - 12 символов UUID

### Haptic Feedback
- Клик на комнату: `impact` + `light`
- Создание комнаты: `notification` + `success`
- Ошибка: `notification` + `error`
- Генерация ссылки: `impact` + `medium`

---

## 📊 MongoDB Коллекции

### Коллекция `rooms`:
```javascript
{
  room_id: "uuid",
  name: "Экзамен по математике",
  description: "Подготовка к экзамену",
  owner_id: 123456789,
  created_at: ISODate(),
  updated_at: ISODate(),
  participants: [
    {
      telegram_id: 123456789,
      username: "user1",
      first_name: "Иван",
      joined_at: ISODate(),
      role: "owner",
      referral_code: null
    },
    {
      telegram_id: 987654321,
      username: "user2",
      first_name: "Мария",
      joined_at: ISODate(),
      role: "member",
      referral_code: "123456789"
    }
  ],
  invite_token: "abc123xyz456",
  color: "blue"
}
```

### Обновленная коллекция `group_tasks`:
```javascript
{
  task_id: "uuid",
  title: "Решить главу 1",
  description: "Задачи 1-20",
  room_id: "room_uuid",  // НОВОЕ ПОЛЕ
  owner_id: 123456789,
  deadline: ISODate(),
  category: "study",
  priority: "high",
  status: "in_progress",
  participants: [...],
  created_at: ISODate(),
  updated_at: ISODate()
}
```

---

## 🧪 Тестирование

### Backend тесты (через curl):

1. **Создать комнату**:
```bash
curl -X POST http://localhost:8001/api/rooms \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Экзамен по математике",
    "description": "Подготовка к экзамену",
    "telegram_id": 123456789,
    "color": "blue"
  }'
```

2. **Получить комнаты пользователя**:
```bash
curl http://localhost:8001/api/rooms/123456789
```

3. **Сгенерировать ссылку-приглашение**:
```bash
curl -X POST http://localhost:8001/api/rooms/{room_id}/invite-link \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789}'
```

4. **Присоединиться к комнате**:
```bash
curl -X POST http://localhost:8001/api/rooms/join/{invite_token} \
  -H "Content-Type: application/json" \
  -d '{
    "invite_token": "abc123xyz456",
    "telegram_id": 987654321,
    "username": "user2",
    "first_name": "Мария",
    "referral_code": "123456789"
  }'
```

5. **Создать задачу в комнате**:
```bash
curl -X POST http://localhost:8001/api/rooms/{room_id}/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "room_id": "room_uuid",
    "title": "Решить главу 1",
    "description": "Задачи 1-20",
    "deadline": "2025-12-31T23:59:59",
    "category": "study",
    "priority": "high",
    "telegram_id": 123456789
  }'
```

### Frontend тесты:
1. ✅ Отображение секции "Комнаты"
2. ✅ Горизонтальный скролл карточек
3. ✅ Создание комнаты через модальное окно
4. ✅ Открытие деталей комнаты
5. ✅ Генерация и копирование ссылки-приглашения
6. ✅ Добавление задачи в комнату
7. ✅ Отображение прогресса комнаты
8. ✅ Удаление комнаты (владелец)
9. ✅ Выход из комнаты (участник)

---

## ⚙️ Конфигурация

### Лимиты
- ❌ Нет лимитов на количество комнат
- ❌ Нет лимитов на количество задач в комнате
- ❌ Нет лимитов на количество участников (было: до 10)

### Права доступа
- **Владелец (owner)**:
  - Удалить комнату
  - Генерировать ссылку-приглашение
  - Создавать задачи
  - Не может покинуть комнату (только удалить)

- **Участник (member)**:
  - Генерировать ссылку-приглашение
  - Создавать задачи
  - Покинуть комнату
  - Не может удалить комнату

---

## 🚀 Следующие шаги (опционально)

### Возможные улучшения:
1. 🔔 Push-уведомления о событиях в комнате
2. 💬 Чат комнаты (расширение комментариев)
3. 📈 Аналитика активности участников
4. 🎨 Кастомизация цвета комнаты
5. 📌 Закрепление важных задач
6. 🔍 Поиск по задачам комнаты
7. 📅 Календарь дедлайнов комнаты
8. 🏆 Рейтинг участников по выполненным задачам
9. 🔄 Синхронизация с Google Calendar
10. 📊 Экспорт отчетов о прогрессе

---

## 📝 Итоги

✅ **Backend**: 9 API endpoints + 7 моделей данных  
✅ **Frontend**: 3 компонента + 1 API service  
✅ **Интеграция**: Полная интеграция в TasksSection  
✅ **Дизайн**: Синий градиент, адаптивный, с анимациями  
✅ **Функционал**: Создание, приглашения, задачи, удаление  

**Статус**: 🎉 Полностью готово к тестированию!
