# Команда /users для Telegram Бота

## 📋 Описание

Добавлена команда `/users` для вывода списка всех пользователей прямо в Telegram боте.

## ✅ Реализовано

### 1. Команда /users в telegram_bot.py

**Функциональность:**
- Показывает полный список пользователей из базы данных
- Доступна только для администраторов (ID: 765963392)
- Выводит детальную информацию о каждом пользователе

**Информация о пользователе:**
- Полное имя (first_name + last_name)
- Username (@username)
- Telegram ID
- Название группы
- Дата регистрации
- Последняя активность (сегодня/вчера/N дней назад)

**Безопасность:**
- Проверка прав администратора перед выполнением
- Неавторизованные пользователи получают сообщение об отказе в доступе

**Разбивка длинных списков:**
- Автоматическая разбивка на несколько сообщений если список превышает 4096 символов (лимит Telegram)

### 2. Исправлена админ панель в приложении

**Проблема:**
Админ панель в веб-приложении не загружала данные из-за неправильного BACKEND_URL в production окружении.

**Решение:**
Обновлена логика определения backend URL в `AdminPanel.jsx`:
- Для localhost: используется `http://localhost:8001`
- Для production: используется `window.location.origin` (текущий домен)
- Правильная маршрутизация через Kubernetes ingress

## 🚀 Использование

### В Telegram боте:

```
/users
```

**Ответ для администратора:**
```
👥 Список пользователей (2 чел.)

1. Иван Петров (@ivan123)
   ID: 765963392
   Группа: СААад-01-24
   Регистрация: 13.11.2025
   Активность: сегодня

2. Мария Сидорова (@maria_s)
   ID: 6641063074
   Группа: Не выбрана
   Регистрация: 13.11.2025
   Активность: вчера
```

**Ответ для обычного пользователя:**
```
❌ У вас нет прав для использования этой команды.
```

### В веб-приложении:

1. Откройте приложение с admin ID (765963392)
2. Прокрутите вниз до кнопки "📊 Админ панель"
3. Нажмите на кнопку
4. Админ панель загрузит данные с сервера и отобразит:
   - Общую статистику (всего пользователей, активные, новые)
   - Графики активности
   - Топ пользователей
   - Распределение по факультетам и курсам

## 🔧 Технические детали

### Файлы изменены:
- `/app/backend/telegram_bot.py` - добавлена команда users_command()
- `/app/frontend/src/components/AdminPanel.jsx` - исправлен BACKEND_URL

### Константы:
```python
# Администраторы, которые могут использовать /users
ADMIN_IDS = [765963392]
```

### API Endpoints используемые админ панелью:
- `GET /api/admin/stats` - общая статистика
- `GET /api/admin/users-activity` - активность по дням
- `GET /api/admin/hourly-activity` - активность по часам
- `GET /api/admin/weekly-activity` - активность по дням недели
- `GET /api/admin/feature-usage` - использование функций
- `GET /api/admin/top-users` - топ пользователей
- `GET /api/admin/faculty-stats` - распределение по факультетам
- `GET /api/admin/course-stats` - распределение по курсам

## 📝 Логи

**Успешное выполнение:**
```
2025-11-13 04:01:12,951 - __main__ - INFO - Команда /users от администратора: 765963392 (@shkarol)
2025-11-13 04:01:13,107 - __main__ - INFO - ✅ Отправлен список из 2 пользователей администратору 765963392
```

**Неавторизованная попытка:**
```
2025-11-13 XX:XX:XX,XXX - __main__ - WARNING - Неавторизованная попытка использовать /users от 123456789 (@test_user)
```

## 🎯 Результат

✅ Команда `/users` работает в Telegram боте
✅ Админ панель в веб-приложении теперь корректно загружает данные
✅ Администратор может просматривать пользователей в двух местах: в боте и в приложении
